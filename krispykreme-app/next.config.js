/** @type {import('next').NextConfig} */
const nextConfig = {
    // Set the srcDir to tell Next.js where to find your pages or app directory
    srcDir: 'src',
  };
  
  module.exports = nextConfig;
  